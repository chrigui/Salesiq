import { ContinueExperience } from "@/components/display/ContinueExperience";

export const metadata = {
  title: "Continue on your phone · SalesIQ",
};

export default function ContinuePage() {
  return <ContinueExperience />;
}
